import { TempoDeFilmePipe } from './tempo-de-filme.pipe';

describe('TempoDeFilmePipe', () => {
  it('create an instance', () => {
    const pipe = new TempoDeFilmePipe();
    expect(pipe).toBeTruthy();
  });
});
